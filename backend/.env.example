# Copy this file to .env and fill in real values. .env is gitignored.

# Required — LLM API key (OpenRouter is OpenAI-API-compatible).
OPENROUTER_API_KEY=

# Optional — override Django's dev defaults. Leave unset for local development.
# DJANGO_SECRET_KEY=
# DJANGO_DEBUG=True
# DJANGO_ALLOWED_HOSTS=
